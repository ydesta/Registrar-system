# Security fix: Configure npm to use exact versions and prevent dependency confusion
save-exact=true
package-lock=true

# Only install packages from official npm registry
registry=https://registry.npmjs.org/

# Prevent automatic updates that could introduce malicious packages
engine-strict=true

# Set package lock file to be committed
save-prefix=""

